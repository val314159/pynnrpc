import nn

if __name__=='__main__':      
    print "sub"

    s = nn.Socket(nn.SUB)
    s.connect('tcp://127.0.0.1:6234')
    s.set_string_option(nn.SUB, nn.SUB_SUBSCRIBE, '')
    while 1:
        print "!", s.recv()
